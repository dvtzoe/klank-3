[project]
name = "klank-3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "numpy>=2.3.5",
    "openai>=2.14.0",
    "pyaudio>=0.2.14",
    "setuptools>=80.9.0",
    "silero-vad>=5.1.2",
    "soundfile>=0.13.1",
    "torch>=2.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]

[tool.ruff.lint]
extend-select = ["I"]
