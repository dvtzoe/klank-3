[project]
name = "klank-3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["dotenv>=0.9.9", "openai>=2.14.0"]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]

[tool.ruff.lint]
extend-select = ["I"]
